# Changelog

이 프로젝트는 안정 릴리스를 `main` 브랜치에 유지하고, 기능 추가는 별도 브랜치에서 검증한 뒤 병합합니다.

## [v1.0.16] - 2026-08-28

### Added

- **저장된 위치의 맵을 그 자리에서 뗄 수 있습니다.** 맵이 있는 위치 행에 `Map ✕`
  버튼이 생겼습니다. 그 위치에서만 맵을 제거하고, 다른 위치가 같은 이미지를 쓰지
  않으면 장치가 파일도 지웁니다. 떼는 맵이 지금 화면에 깔린 맵이면 문자판도 같은
  요청에서 민 다이얼로 돌아갑니다. 이전에는 위치를 통째로 지우는 것 말고는 맵만
  없앨 길이 없었습니다.

### Changed

- Background Image의 `Clear` 버튼을 **`Disable`**로 바꿨습니다. 하는 일은 원래
  그대로였는데(현재 화면만 끄고 위치의 이미지는 보존) 이름이 "지운다"로 읽혀서,
  실제로는 있는 기능이 없는 것처럼 보였습니다. 안내문도 함께 정리했습니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.16.bin`
  - SHA256: `61C854C305BABC217B56862538FC77988EC9F47B6AD493E118731AAA70F84827`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.16.bin`
  - SHA256: `2A70F689AA1ECBA543B03C41F7699076C2184415180EEEF2FE5F31509C1ED930`
- 플래시 81.0%, RAM 51.6%. 경고 없이 빌드됩니다.

## [v1.0.15] - 2026-08-28

### Changed

- **겹친 라벨이 폴마다 번갈아 나옵니다.** 라벨 배치가 늘 가까운 기체부터라, 붙어
  나는 두 기체 중 먼 쪽은 매 폴마다 줄을 빼앗겨 항공사·노선이 영영 안 나왔습니다.
  배치 시작점을 폴마다 한 칸씩 돌려서 충돌한 이웃끼리 차례를 바꿉니다 — 이번
  회전엔 이쪽이 전체 라벨, 다음 회전엔 저쪽. 주변이 빈 기체는 순서와 무관하게
  전체 라벨 그대로입니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.15.bin`
  - SHA256: `180E70B08A3EF39199CA23C30C01F238998BBEC1C445624CA5FC64EA12EAB2CE`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.15.bin`
  - SHA256: `44FD8DC83FC5A94CFC7E3DFAD141C9ABD59995F2356E4113FEA74B8DE51B5B1E`
- 플래시 81.0%, RAM 51.5%. 경고 없이 빌드됩니다.

## [v1.0.14] - 2026-08-28

### Changed

- **지도 화면에서 링 밖 항공기도 정식 라벨을 답니다** — 항공사, 편명, 고도, 노선까지
  안쪽 기체와 똑같이. 지도에서는 링 밖 기체가 이미 실루엣으로 그려지고 있었는데
  이름표만 없었습니다. 민 문자판(동심원)에서는 종전 그대로 점입니다.
- **지도 화면의 조회 여유를 +1해리 → +2해리**로 늘렸습니다. 15km 설정이면 18.5km까지
  조회합니다. 링 밖 기체가 라벨을 달게 됐으니 더 일찍 보이는 값이 생겼습니다.
  민 문자판은 종전 그대로 +1해리입니다.
- 라벨 상자를 화면 안으로 밀어 넣지 않습니다. 오른쪽이 모자라면 왼쪽으로 뒤집는
  것까지만 하고, 그래도 넘치면 **넘친 채로 보이는 부분만** 그립니다. 픽셀이 전부
  drawPixel을 지나므로 패널이 알아서 자릅니다. 상자를 강제로 안으로 밀면 라벨이
  자기 기체에서 눈에 띄게 떨어져, 잘린 단어보다 나쁘게 읽혔습니다.

실측: 15km 링에서 17.2km(제주항공)·17.5km(아시아나항공) 기체가 항공사명을 달고
잡혔습니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.14.bin`
  - SHA256: `D901A6D1F3EE159D39EF4CFE2F07A38BDDBE6500BED80CEE3543DBE6D9DDCC4C`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.14.bin`
  - SHA256: `9C5589FE5E3BFA69FD8556DE09C7394A66B7679E1FEEFC5B0B2A524FB0C8BF40`
- 플래시 81.0%, RAM 51.5%. 경고 없이 빌드됩니다.

## [v1.0.13] - 2026-08-28

### Added

- **WiFi 프로파일 최대 5개.** System 메뉴에 이름·비밀번호로 저장해 두면, 부팅 때
  기본 SSID가 실패한 뒤 순서대로 시도합니다 — 장치를 집과 회사 사이에서 옮겨
  꽂기만 하면 됩니다. 전부 실패했을 때만 안내 화면과 복구 AP가 올라옵니다.
- 시도 전에 **스캔을 한 번** 합니다. 없는 망에 눈감고 붙는 비용이 15초, 스캔은
  2초에 어떤 프로파일이 전파 중인지 알려줍니다. 스캔에 안 보인 프로파일도 그다음에
  차례가 옵니다 — 숨김 SSID는 스캔에 원래 안 나옵니다.
- `/api/config`는 프로파일 **이름만** 돌려줍니다. 비밀번호는 장치에만 있습니다.
  비밀번호 없이 다시 저장된 항목은 같은 SSID의 저장된 비밀번호를 유지합니다 —
  페이지가 비밀번호를 받은 적이 없으니 되돌려 보낼 수도 없기 때문입니다.

### Fixed

- 컴파일 시 폴백 SSID가 빈 문자열인데도 시도해서, 실패한 부팅마다 15초를
  `""`에 붙어 보겠다고 쓰던 문제. 이름이 있을 때만 시도합니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.13.bin`
  - SHA256: `EBACE3BA526F30B4E16171F40449D73B825619A9C6418D80DDA2F80A0C49F005`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.13.bin`
  - SHA256: `8879DED9B009B7E8DB3A91A28C420B2383B2D9D7CDE4CB20A12CCA4435E42197`
- 플래시 81.0%, RAM 51.6%(프로파일 5칸 = 정적 490B). 경고 없이 빌드됩니다.

## [v1.0.12] - 2026-08-28

### Added

- **새 화면: Luftwaffe Junghans Borduhr.** 항공 크로노그래프 실물로 만든 시계입니다.
  문자판은 소유자가 준비한 240×240 사진(`/faces/borduhr.rgb`), 바늘 넷도 실물 바늘
  사진입니다 — 투명 배경에서 잘라 **패널 2배 해상도**로 굽고, 색을 알파에 사전곱해
  저장한 뒤 그릴 때 색·알파 모두 이중선형으로 샘플링합니다. 처음엔 패널 해상도에
  최근접 샘플링으로 했더니 11px짜리 바늘이 회전할 때마다 톱날이 됐습니다.
- 지우기 단계가 없습니다. 사각형을 위에서부터 다시 만듭니다 — 플래시에서 문자판
  한 줄을 읽고, 그 줄을 지나는 바늘을 섞고, 밀어 넣습니다. 남는 것이 원천적으로
  없습니다. 매초 초침이 떠난 자리와 밟은 자리만, 분이 바뀌면 나머지도 다시 그립니다.
- 12시의 빨간 지침은 문자판에서 도드라진 부품이라 **초침이 그 아래로 지나갑니다**.
  바늘을 다 얹은 뒤 그 사각형만 문자판으로 되돌립니다.
- 작은 원(30분 카운터)의 바늘은 실물 사진 그대로 4와 6 사이에 서 있습니다.
  크로노그래프의 경과분 카운터라 시각과 무관합니다.
- 바늘 비율은 소유자의 확대 사진에서 랜드마크로 실측했습니다: 분침 끝=분 눈금 링,
  시침 어깨=숫자 하단, 초침 끝=분침 끝, 마름모는 끝에서 4px, 검은 꼬리는 레지스터
  안쪽까지. **다른 사진에서 잰 픽셀 길이를 옮기면 안 됩니다** — 사진마다 크롭 배율이
  다릅니다. v1.0.12 전의 시도 둘이 그래서 틀렸습니다.

### Fixed

- **분침이 반쪽씩 움직이던 문제.** 바늘 각도는 매초 갱신되는데 다시 칠하는 범위는
  초침이 지나간 사각형뿐이라, 분침에 초 분수(0.1°/s)가 섞여 있으면 초침이 가로지른
  부분만 새 각도로 찍혔습니다. 분침을 분 단위로 양자화해 그려진 각도와 계산 각도가
  항상 일치하게 했습니다.
- NTP 보정이 같은 분 숫자에 착지하면 시침이 옛 시각에 최대 1분 서 있던 문제.
  시가 바뀌어도 전체를 다시 그립니다.
- 문자판 읽기가 중간에 끊기면(날씨 조회가 파일을 놓게 함) 덜 그린 줄이 낡은 채
  남던 문제. 짧은 읽기는 전체 재도장을 예약합니다.
- Borduhr 화면에서 IP 배지를 매 루프 다시 그리던 문제(레이더 v1.0.10과 같은 결함).
- 초침 검은 꼬리의 누끼 림(밝기 90, 문자판 15)이 흰 테두리로 떠 보이던 문제.
  지우지 않고 스푼 몸통 톤(≤45)으로 눌러 금속 테두리 질감만 남겼습니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.12.bin`
  - SHA256: `7F448ACF5235B5F77F03AC8762AE6B3B4595DC507F78BAB335A01E4820A99143`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.12.bin`
  - SHA256: `E263816A3D7EEB883B2CDE884F4529778F20A12339816A1C8C8CF35F5C398011`
- 바늘 스프라이트 52.6KB 포함 플래시 80.8%, RAM 50.9%. 경고 없이 빌드됩니다.

## [v1.0.11] - 2026-08-28

### Added

- **Clock / Weather에 저장된 위치.** 레이더가 하던 것과 같습니다. 이름을 붙여 격자
  좌표를 저장해 두었다가 Load 한 번으로 불러옵니다 — 백석동 57/128, 서초동 60/125
  처럼. 최대 6개, 설정 키 `weather_presets`, 재부팅과 펌웨어 OTA를 견딥니다.
  불러오면 그 자리에서 기상청에 다시 물어봅니다.
- 저장 항목은 **이름·격자 좌표·화면에 찍는 지역명** 셋입니다. 이름과 지역명을 따로
  두는 이유는, 이름은 브라우저에서만 쓰이니 아무거나 되지만 지역명은 패널에 그려야
  하고 패널은 이 빌드가 구워 넣은 한글만 그릴 수 있기 때문입니다. 웹에서 `백석동`
  이라 부르는 곳을 화면에는 `Baekseok`으로 띄울 수 있습니다.
- API 키와 타임존은 저장하지 않습니다. 계정과 시계를 설명하는 값이지 장소를 설명하는
  값이 아닙니다.
- 저장할 때 격자를 **1~149 / 1~253**로 검사합니다. 기상청 단기예보 격자가 그 크기라
  벗어난 좌표는 장소가 아니고, 조회하면 이유도 말하지 않은 채 매번 빈 응답이 옵니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.11.bin`
  - SHA256: `DC4A77F1B1A3972EB4D39703D2280E991D519C9611E6031EEC6C2117167E1042`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.11.bin`
  - SHA256: `0CEE97BE8F067F1F4F2416B1669F413C96F2E736CA99D897FBF540152E9E4FFB`
- 정적 RAM 41,020 → 41,380바이트(50.1% → 50.5%). 저장 슬롯 6개 값입니다.
- 플래시 사용량 75.4%. 경고 없이 빌드됩니다.

## [v1.0.10] - 2026-08-28

레이더 화면만 다시 훑어 나온 세 가지입니다. 기능 변화는 없습니다.

### Fixed

- **속도 벡터가 지우는 상자 밖에 그려지던 문제.** 지우개는 심벌 중심에서 ±14px
  사각형인데 벡터는 최대 24px까지 뻗습니다. 지도를 깔면 갱신이 바뀐 기체만 지우므로
  바깥쪽 10px이 매번 남아, 지나간 자리마다 자홍색 토막이 쌓였습니다. 민 문자판에서는
  스윕이 그 픽셀을 지우지만 복구는 헐로 판정하므로 다시 그리지 않아 벡터가
  갉아먹혔습니다. 이제 벡터 끝점을 플롯이 계산하고 헐이 그것까지 덮습니다 —
  그리는 쪽과 지우는 쪽이 같은 산술을 씁니다.
- **지도 위에서 겹친 라벨이 파먹힌 채 남던 문제.** 채워진 실루엣과 불투명한 글자는
  아래 있는 것을 덮습니다. 옆 기체가 다시 그려지며 안 움직인 기체의 라벨을 덮어써도,
  그 기체는 바뀐 게 없으니 다시 그려지지 않아 손상이 다음 이동까지 남았습니다.
  지우기가 문 자리를 되살리던 규칙을 그리기에도 적용하고, 되살린 기체가 또 다른
  기체를 덮으므로 더 번질 곳이 없을 때까지 전파시킵니다.
- **레이더 화면에서 IP 배지를 매 루프마다 다시 그리던 문제.** 다른 화면은 1초 게이트
  뒤에 있는데 레이더 경로만 무조건 호출하고 있었습니다. 스윕은 41ms에 한 프레임인데
  그 사이 수천 번, 글리프 13개를 패널에 얹고 있었습니다. `drawRadar()`가 실제로
  그렸는지를 돌려주고 그렸을 때만 배지를 얹습니다. 배지는 프레임이 아니라 복구입니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.10.bin`
  - SHA256: `9ADFD7AF20034AB18FC9D6E8D5E1E26617AE9D346BCAEC25D4FDB7E5FD44053C`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.10.bin`
  - SHA256: `B51E657F396F37C65082B0BDF252597979A31E7721363EEB79DC6001FC6DA187`
- 플래시 사용량 75.3%, RAM 50.1%. 경고 없이 빌드됩니다.

## [v1.0.9] - 2026-08-28

### Added

- **레이더 배경 지도.** 아무 이미지나 올리면 브라우저가 240×240으로 맞춰 보내고
  장치가 문자판 아래에 **35% 밝기**로 깝니다. 지도가 있으면 동심원·십자·스윕선을
  그리지 않습니다 — 지도 자체가 위치를 말하므로 그 위의 문자판 가구는 방해입니다.
  `N` 마커만 남습니다. 저장된 위치마다 각자의 지도를 갖습니다.
- **동심원을 5km 단위**로 그립니다. 반경이 길어 5km 원이 붙으면 간격을 올립니다.
- **WiFi 연결 실패 안내 화면.** 이전에는 마지막 화면을 그대로 띄워서 잘 도는 시계와
  구별되지 않았습니다. 이제 복구용 AP를 자동으로 올리고 접속할 무선망 이름·주소·
  다음에 할 일을 화면에 띄웁니다. 부팅 시 실패했거나, 돌던 중 끊기고 12초가 지나면
  올라오고, WiFi가 돌아오면 화면과 AP 모두 내려갑니다.
- **재시작 후 3분간 좌하단에 장치 IP**를 모든 화면에 표시합니다.
- 항공기 마커를 **실루엣**(기수·후퇴익·꼬리날개)으로 바꿔 진행 방향이 한눈에 보입니다.

### Changed

- **복구용 AP가 상시 켜져 있지 않습니다.** 예전에는 WiFi에 정상 접속해도 개방망
  `SDP-Recovery`를 함께 띄웠고, 그 뒤에 OTA·파일 쓰기·포맷·raw 포트가 무인증으로
  열려 있었습니다. 이제 들어올 길이 필요할 때만 올라옵니다. 설정 항목은 없앴습니다 —
  장치가 더 잘 판단합니다.
- 편명을 항공사·고도와 **같은 크기**로 낮췄습니다. 라벨이 세 줄이 되면서 큰 가운데
  줄이 문자판을 다 차지해, 붐빌 때 오히려 읽히지 않았습니다.
- 공항 표 210곳(NLU 멕시코시티, 나가사키, 다퉁, 허페이 추가).

### Fixed

- **재부팅 후 날씨가 갱신되지 않던 문제(Codex 진단·수정).** 관측값과 예보는 호출이
  둘인데 첫 번째만 성공하면 전체를 성공으로 쳐서 `status`를 `ok`로 덮고
  `lastWeatherMs`까지 찍었습니다. 예보가 비어도 30분간 잠겼습니다. 예보 파싱에
  ArduinoJson 필터를 씌우고, 유효한 예보가 없으면 성공으로 치지 않습니다.
- **예보가 `NoMemory`로 실패하던 문제.** `httpGetBody`가 응답 크기와 무관하게 매번
  14KB를 예약하고 관측값·예보 본문이 동시에 살아 있어, 30KB가 비어 있는데도 파서가
  자랄 자리가 없었습니다. 실측치(관측 1.5KB, 예보 5.5KB)에 맞춰 예약하고, 관측값
  버퍼와 레이더 배경 파일을 예보 요청 전에 반납합니다.
- **날씨 재시도가 무한정이던 문제.** 실패해도 `lastWeatherMs`가 안 움직여 15초마다
  KMA를 두 번씩 불렀습니다(하루 11,000건). 연속 실패마다 대기가 두 배로 늘어납니다.
- **기수 방향이 화면 회전을 따르지 않던 문제.** 위치는 `radarScreenDeg()`로 돌리면서
  항적은 원래 방위를 그대로 써서, 340°로 돌린 문자판에서 모든 기체가 20도 어긋나
  있었습니다.
- **좌상단 헤더가 지워진 채 남던 문제.** 기체 라벨을 지운 사각형이 헤더를 물었는데,
  헤더는 "글자가 그대로면 건너뛴다"라 복구하지 못했습니다. 손상을 감지하는 대신
  매 프레임 다시 쓰게 했습니다 — 글리프는 안 켜진 픽셀을 건드리지 않으므로 같은
  글자를 덮어써도 화면은 그대로이고 지워진 부분만 살아납니다.
- **동심원이 도려내진 채 남던 문제.** 지우기를 `radarDrawRings()`보다 뒤로 옮겨야
  하는데 앞에 두어, 그린 직후 지워지고 아무도 되돌리지 않았습니다.
- 지도 위 리페인트가 오래 걸리던 문제. 지우개가 라벨 5단계 변형의 합집합(화면 폭
  대부분)을 쓰고 있었습니다. 실제로 그린 상자만 지우고, 바뀐 기체만 다시 그립니다.
- 편명 없는 기체가 심벌만 그려지던 문제. 미설정 트랜스폰더는 편명 칸을 공백이나
  `@` 여덟 개로 방송하는데 `|` 폴백이 걸리지 않았습니다.

### Notes

- **메모리 정리.** 정적 RAM 41,852 → 41,020바이트. 레이더 배열 상한을 24 → 16으로
  낮췄습니다(라벨 디클러터가 그만큼 배치하지 못하고, 40km 링에서도 10대였습니다).
- `radar_presets`가 좌표·반경·화면 방위·최저 고도·배경 이미지를 함께 담습니다.
- 참조되지 않는 배경 이미지는 설정 변경 시와 부팅 시 자동 삭제됩니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.9.bin`
  - SHA256: `9D68D2956651C3573E17F097FA8665E564CCA6F1A0E6DD2D6D0D106CE0F79086`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.9.bin`
  - SHA256: `71AD60B6631BF0333560B921F59FF57444BDBDA0B32681C6BC934EB33525623A`
- 플래시 사용량 75.3%, RAM 50.1%. 경고 없이 빌드됩니다.

## [v1.0.8] - 2026-08-28

### Added

- **레이더 위치 프리셋.** 장소에 따라 달라지는 것 전부 — 좌표, 반경, 화면 방위,
  최저 고도 — 를 이름 하나(예: 회사, 집)에 묶어 최대 6개 저장하고, Load 한 번으로
  그대로 적용합니다. 설정 키 `radar_presets`, 재부팅과 펌웨어 OTA를 견딥니다.
- **출발지 표시.** 경로가 `아오모리-김포`처럼 양 끝으로 나옵니다. 출발지는 양쪽을
  다 알 때만 붙습니다 — 한쪽 빠진 대시는 이름 하나보다 정보가 적습니다.
- **공항 206곳, 항공사 125곳.** 소유자가 정리한 한국 직항 공항표와 취항 항공사표
  (인천공항 공식 취항정보)를 흡수했습니다. 반영 중 세 건 정정: BSZ→FRU(비슈케크
  마나스의 실제 IATA), 3SX→BOX(에어로로직의 실제 ICAO), CAO를 에어차이나카고로
  (CKK "중국화물항공"과 다른 항공사인데 이름이 겹칠 뻔).
- 회전익 기종명 정정: AS50은 AS350 계열 부호이고 현행 생산형이 H125라
  **에어버스H125**로 표기합니다. "에어버스350"은 A350 여객기로 읽혔습니다.
  2015년 개명을 계열 전체(H130~H155)에 적용했습니다.

### Fixed

- **붐비면 라벨이 사라지던 문제.** 겹치면 통째로 포기하던 것을, 출발지 → 목적지 →
  항공사 순으로 한 줄씩 내려놓고 편명만은 겹쳐서라도 그리게 했습니다. 축약된
  라벨이 지우개 범위 밖에 그려질 수 있던 것도 헐을 전 단계 합집합으로 바꿔 막았습니다.
- **편명 없는 기체가 심벌만 그려지던 문제.** 미설정 트랜스폰더는 편명 칸을 공백
  여덟 개나 `@` 여덟 개로 방송하는데, ArduinoJson의 `|` 폴백은 키가 없을 때만
  작동해 둘 다 통과했습니다. 영숫자가 없으면 hex를 이름으로 씁니다.
- **헬리콥터가 비행기로 그려지던 문제.** 카테고리는 트랜스폰더 설정값이라 Bell 206이
  `A1`("경항공기")로 날아옵니다. 기종 표가 아는 코드면 카테고리와 무관하게
  회전익으로 처리합니다.
- **스윕이 매 스텝 전체 플롯을 재계산하던 문제.** 표가 커지며 한 플롯이 PROGMEM
  스캔 3회 + 5단 헐 합집합이 됐는데 회전당 240스텝 × 기체수 × 2회씩 다시 만들고
  있었습니다. 확정된 상자·헐을 캐시하고 스윕이 지난 기체만 다시 그립니다.
  리페인트 60ms → 14ms.
- 생성기가 표 256개째에서 거부합니다. `COUNT`가 `uint8_t`라 넘치면 0으로 감겨
  모든 라벨이 조용히 코드로 돌아갑니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.8.bin`
  - SHA256: `38427F74701461513244AFFC04923880024B2C03586E39DAABC4B3A61F4663FF`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.8.bin`
  - SHA256: `816F7453D44B387949FE3591C50C9AE9BEEA4CACC274A29EF458241D24BDBA8F`
- 플래시 사용량 74.5%, RAM 49.5%. 경고 없이 빌드됩니다.

## [v1.0.7] - 2026-08-27

### Fixed

- **레이더가 항공기를 하나도 못 찾던 문제.** `opendata.adsb.fi`가 이날부터
  `Transfer-Encoding: chunked`로 답하기 시작했는데, 본문을 JSON 파서에 그대로
  흘려 넣는 구조라 `http.getStream()`이 청크를 풀지 않습니다. 파서가 `{`가 아니라
  청크 길이 줄 `60d`부터 읽어 매번 실패했습니다. 하늘이 비었든 34대가 있든
  무관했습니다. 두 피드 모두 HTTP/1.0으로 요청해 chunked 자체를 없앱니다.
  경로 조회(`api.adsbdb.com`)는 아직 `Content-Length`를 쓰지만 같은 잠재
  버그라 함께 막았습니다.
- **힙 가드가 레이더를 영구히 잠그던 문제.** 웹 파일을 올리면 힙이 갈라져 최대
  블록이 `RADAR_MIN_BLOCK` 바로 아래로 떨어지는 일이 있는데, 그때부터 모든 조회가
  거부되고 거부된 조회는 아무것도 할당하지 않으므로 기다리는 단편화가 영원히
  바뀌지 않습니다. 재부팅 전까지 레이더가 죽어 있었습니다. 연속 6번 거부하면
  한 번은 비켜 줍니다.

### Added

- **목적지를 한글로 표시합니다.** `display/Airports.h`가 IATA 세 글자를 공항·도시
  이름으로 옮깁니다(155개). 표에 없으면 종전대로 코드를 그대로 씁니다.
  캐시는 이름이 아니라 코드를 담아, 표를 다시 뽑아도 낡지 않습니다.

### Changed

- `Accept-Encoding: identity`를 두 조회에 명시합니다. HTTP/1.0을 켜면 HTTPClient가
  자기 `Accept-Encoding` 헤더를 함께 빼는데, 그 헤더가 없는 요청은 서버가 아무
  인코딩이나 골라도 되는 요청입니다. 방금 겪은 사고를 다시 무장시키지 않으려는 것입니다.
- 파싱 뒤 본문을 1바이트씩 비우던 루프를 걷어냈습니다. 연결 재사용을 위한 것이었는데
  HTTP/1.0에는 재사용할 연결이 없어, 폴링마다 최대 200ms를 그냥 쓰고 있었습니다.
- 구울 한글 목록을 생성기가 세 표에서 직접 계산합니다(Small 91자). 손으로 적어 두면
  표가 늘 때 조용히 낡고, 이름 한 글자가 화면에서 그냥 사라집니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.7.bin`
  - SHA256: `0EDF40536A33DED26617AB151C6355B48CD66D5F0B63F526DAF917C3CE1DF420`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.7.bin`
  - SHA256: `3C77D492DB80F49010BD2E5C2FA2824C0239EE2BFC64EAE691B2EE936A832E5D`
- 플래시 사용량 73.6%, RAM 48.5%. 경고 없이 빌드됩니다.

## [v1.0.6] - 2026-08-27

### Changed

- **웹 UI 비밀번호를 소스에서 걷어내 설정으로 옮겼습니다.** `main.cpp`의 `AUTH_PASSWORD`
  상수에 박혀 있어서 빌드된 이미지가 특정 소유자 전용이었습니다. 이제 설정 키
  `web_password`가 갖고, 초기값은 `0000`입니다. 같은 이미지를 누구나 그대로 씁니다.

### Added

- **System 메뉴에서 비밀번호를 바꿉니다.** `POST /api/password`가
  `{"current": "...", "new": "..."}`를 받고 새 값은 1~32자입니다. 성공하면 세션 토큰을
  새로 발급해 다른 브라우저는 모두 로그아웃되고, 방금 바꾼 브라우저만 응답에 실린 새
  쿠키로 유지됩니다. 설정 저장이 실패하면 옛 값으로 되돌리고 500을 돌려줍니다.
- **부팅 후 2분 동안 웹 메뉴가 잠기지 않습니다.** 비밀번호를 잊었을 때 돌아오는
  길입니다. 이 구간에서는 `/api/password`가 `current`를 검사하지 않습니다.
- `/api/config`가 `web_password_is_default`를 알려줍니다. 아직 공장 비밀번호면
  System 카드가 그렇게 안내합니다.

### Fixed

- 무인증 `/api/config` POST는 `web_password`를 받지 않습니다. 받으면 누구나 옛 비밀번호
  없이 바꿀 수 있습니다. `loadConfig()`와 이 핸들러는 대입문이 글자 단위로 같아
  고칠 때 함께 걸리기 쉽습니다.
- `/api/config` GET은 비밀번호를 돌려주지 않습니다. 무인증 엔드포인트라 불리언만
  내보냅니다.

### Notes

- 유예 창은 `millis()` 비교가 아니라 **래치**입니다. `millis() < AUTH_GRACE_MS`로 쓰면
  약 49일마다 카운터가 되감길 때 창이 저절로 2분씩 열립니다. `loop()`가 한 번 닫고
  진짜 재부팅 전에는 다시 열리지 않습니다.
- 업데이트하면 **기존 장치의 비밀번호가 `0000`이 됩니다.** `config.json`에
  `web_password` 키가 없어 기본값이 적용됩니다. System 메뉴에서 바로 바꾸십시오.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.6.bin`
  - SHA256: `6F16DE33C33FF28D7431BF4F2604D039059B6725CF9DBF68216AA57C7CD3D0C9`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.6.bin`
  - SHA256: `C80A18478892BB7A055CC0B7D2B385968300D19AC3E733F2647A42AC13FEFF46`
- 플래시 사용량 72.7%, RAM 48.5%. 경고 없이 빌드됩니다.

## [v1.0.5] - 2026-08-26

### Added

- **항공기 레이더 화면**을 추가했습니다. 기준 좌표를 중심으로 반경 안의 항공기를
  PPI 레이더처럼 그리고, 안테나가 시계방향으로 돌면서 지나간 자리만 갱신합니다.
  중계용 PC 에이전트 없이 장치가 `opendata.adsb.fi`를 직접 조회합니다.
- 상단에 **Radar 메뉴**를 더했습니다. 위도·경도, 반경, 조회 주기, 최저 고도,
  화면 위쪽 방위, 목적지 조회 사용 여부를 정합니다.
- **화면 위쪽이 가리킬 방위**를 지정할 수 있습니다. 장치를 실제 앉은 방향대로 놓기
  위한 것으로, 340을 넣으면 화면 위가 북북서를 향하고 `N` 글자도 같이 돕니다.
- **항공사명을 한글로** 편명 위에 붙입니다(`display/Airlines.h`, 49개). 표에 없는
  호출부호는 종전대로 편명만 나옵니다.
- **회전익을 따로 표시합니다.** ADS-B 카테고리 `A7`로 가려내 다른 표식으로 그리고,
  항공사 자리에 기종 이름을 넣습니다(`display/Rotorcraft.h`, 43종 — H60은 "블랙호크").
- **목적지**를 고도 오른쪽에 IATA 세 글자로 붙입니다. 새 항공기가 나타날 때만
  `api.adsbdb.com`에 한 번 묻고 기억합니다.

### Changed

- 화면 비트마스크에 `bit8`(Plane Radar)이 늘어 `SCREEN_COUNT`가 9가 됐습니다.
- 레이더 화면 전체를 고도 폰트 하나로 통일하고, 동심원·십자·상단 텍스트·`N`을
  스윕선과 같은 초록 계열로 맞췄습니다.
- 고도를 FL 대신 **km**로 보여주고, 편명과 고도 사이를 4픽셀 띄웠습니다.
- 웹 UI 제목과 로그인 화면 문구를 **"ESP 다목적 모니터링 장치"**로 바꿨습니다.

### Fixed

- **스윕 잔상이 꼬리 끝에서 가장 밝던 문제.** 감쇠 색 배열을 거꾸로 인덱싱해
  `RADAR_TRAIL_TINT[t-1]`을 쓰고 있었습니다.
- **스윕이 12시에서 화면 전체를 깜빡이던 문제.** 한 바퀴마다 `fillScreen`으로
  다시 그렸습니다. 지나간 반경과 낡은 라벨만 지우는 제자리 갱신으로 바꿨습니다.
- **정해진 방위에서 스윕이 끊기던 문제.** 조회 주기와 한 바퀴가 같아 0.4~0.9초의
  조회가 늘 같은 각도에 떨어졌습니다. 조회를 12시 통과에 걸었습니다.
- **스윕이 되감을 때 지우는 선이 1픽셀 어긋나던 문제.** 부동소수 누산과 360도
  wrap이 겹친 것으로, 정수 스텝으로 바꿨습니다.
- **스윕이 고도 글씨를 지날 때마다 글씨가 깜빡이던 문제.** 라벨 상자를 편명만으로
  재서 더 긴 고도 줄이 상자 밖으로 나갔습니다.
- **재부팅 직후 System 화면 폰트가 커져 있던 문제.** `tft.setTextSize()`가 전역
  상태인데 레이더가 되돌리지 않았습니다.
- **대시보드 우상단 아이콘이 늘 맑음이던 문제.** 초단기실황에는 SKY가 없어
  기본값 그대로였습니다. 초단기예보의 첫 유효 구간에서 가져옵니다.
- **목적지 조회 실패가 큐를 막던 문제.** 실패가 아무것도 기록하지 않아 같은 편명을
  매 바퀴 다시 물었고, 그 뒤의 항공기는 영원히 차례가 오지 않았습니다. 실패도
  "항로 없음"으로 적어 큐가 넘어가게 했습니다.
- **캐시 축출이 49일 후 뒤집히던 문제.** 마지막 사용 시각을 `millis()`로 재서
  wrap 이후에는 가장 새 항목을 버리게 됩니다. 증가만 하는 카운터로 바꿨습니다.
- **`Airlines.h`의 PROGMEM 표가 DRAM을 먹던 문제.** PROGMEM 배열에 평범한 리터럴의
  포인터를 담아 문자열 자체는 DRAM에 남아 있었습니다. `strncpy_P`로 고치고 정적
  RAM 784바이트를 되찾았습니다.
- `/api/radar`가 라벨을 엉뚱하게 보고하던 문제. ArduinoJson이 `const char*`를
  복사하지 않고 참조로 담아, 이미 덮어쓴 버퍼를 가리켰습니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.5.bin`
  - SHA256: `F467E30BB535FD552DE77CCB602ED34728F7A1A7A3C2EB7C5FDD3572D2BE0C63`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.5.bin`
  - SHA256: `02B45D9D655A534C78D8188E9831D8D4C36EFFCD31D395F92005889BFEAC967B`
- 플래시 사용량 72.6%, RAM 48.4%. 경고 없이 빌드됩니다.

## [v1.0.4] - 2026-08-26

### Added

- **사진 액자 화면**을 추가했습니다. 아무 크기·포맷(jpg/png/gif/webp/bmp)이나 올리면
  브라우저가 정사각으로 자르고 240×240으로 줄여 장치가 그대로 뿌릴 수 있는 형식으로
  보냅니다. 장치에는 이미지 디코더가 없습니다 — 240×240×2가 115KB인데 전체 RAM이
  80KB라 디코딩된 프레임을 들 수 없습니다. 사진 한 장 표시에 62ms.
- 상단에 **Album 메뉴**를 더했습니다. 끌어 놓기·붙여넣기로 여러 장을 한번에 올리고,
  썸네일 그리드에서 순서를 바꾸거나 개별로 끄고 지웁니다. 남은 공간과 "몇 장 더"를
  함께 보여줍니다.
- **화면 전환 순서**를 지정할 수 있게 했습니다. 비트마스크는 어떤 화면을 쓸지만
  말할 수 있어서, 순서는 `screen_order` 배열이 따로 가집니다. System 메뉴에서 행을
  끌어 옮깁니다.
- 날씨·날짜 디지털 화면의 **구분자(:)가 1초에 한 번 점멸**합니다. 두 화면은 분 단위로만
  다시 그리므로 점 두 개만 따로 칠합니다.

### Changed

- 화면 비트마스크를 `uint16_t`로 넓혔습니다. 여덟 화면이 정확히 1바이트라, 아홉 번째에서
  최상위 비트를 조용히 잃기 전에 미리 넓혔습니다.
- 날씨 이름에 쓰는 한글 다섯 자를 **맑은 고딕 Bold**로 다시 구웠습니다. 번들 폰트가 Bold인데
  Regular로 구워 넣어 "구름많음"의 `많` 한 자만 얇게 보였습니다. 시스템 한글 폰트 15종을
  번들 글리프와 픽셀 대조해 서체를 특정했습니다.
- 날씨 화면의 **아이콘을 영역 중앙에 놓았습니다.** 고정된 모서리 기준이라 좌측 9px·하단 6px
  치우쳐 있었습니다. 영역의 오른쪽 경계는 가장 긴 날씨 단어 기준으로 고정해, 날씨가
  바뀌어도 아이콘이 움직이지 않습니다.
- 최소 한 화면은 켜져 있어야 합니다. 웹 UI가 마지막 하나의 체크 해제를 거부합니다.
- 복구 포트의 `WiFiServer::available()`을 `accept()`로 바꿨습니다. core 3.x에서 deprecated된
  것이고, 이 경고가 stderr로 나가면 PowerShell이 종료 오류로 취급해 릴리스 빌드가
  멈춥니다.

### Fixed

- **날씨 조회가 부팅 직후 되지 않던 문제.** `setup()`이 NTP 응답 전에 조회해 존재하지 않는
  base_date/base_time으로 요청했고, 실패해도 `lastWeatherMs`가 성공했을 때만 갱신되어
  30분간 재시도가 막혔습니다. 이제 시계가 서면 바로 조회하고, 재부팅 후 24초 안에 `ok`가
  됩니다.
- **조회 실패가 매 루프마다 재시도되던 문제.** 성공만 `lastWeatherMs`를 옮기므로 API가
  죽으면 신선도 조건이 계속 참이라 루프마다 실제 HTTP 요청이 나갔습니다. 재시도에
  15초 간격을 강제합니다.
- **KMA 키가 없는 기기가 매 루프마다 `refreshWeather()`를 호출하던 문제.** 시도 시각을
  가드 절 뒤에서 찍어, 거부된 호출이 재시도 시계를 옮기지 않았습니다. 새로 플래시한
  기기가 바로 이 상태입니다.
- 앨범 목록 저장이 웹 핸들러 안에서 4KB 스택에 576바이트를 잡던 것을 제자리 재작성으로
  바꿨습니다.
- 같은 이름 파일 둘을 같은 밀리초에 올리면 사진 id가 충돌해 하나가 덮이던 문제를
  고쳤습니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.4.bin`
  - SHA256: `BB83B4AFF9AC8821BA9DA7005DD385E5B4FC56B66F2DED854CE5743D0EB46A7C`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.4.bin`
  - SHA256: `FB30527829FE9EDAF917AE75B6DAD6B27AC6469188DD84A362919F9749209CBF`
- 플래시 사용량 59.6%, RAM 44.6%. 경고 없이 빌드됩니다.

### Notes

- LittleFS 이미지는 빌드마다 바이트가 달라집니다. 위 해시는 이 릴리스에 실제로
  올라간 이미지의 것입니다.
- **전체 LittleFS 업로드는 설정과 올려둔 사진을 모두 지웁니다.** 웹 파일만 갱신할 때는
  `/file?path=/web/...`을 씁니다.

## [v1.0.3] - 2026-08-25

### Added

- 시계 화면을 다섯 종 추가했습니다. 전부 웹 UI에서 켜고 끄며 전환 대상에 넣을 수 있습니다.
  - **Mondaine**: 스위스 철도 시계. 눈금과 바, MONDAINE 워드마크와 SBB CFF FFS 표기,
    끝에 원반이 달린 빨간 초침까지 사진 그대로 옮겼습니다. 시침과 분침은 중심을 지나
    뒤로 뻗어 서로 교차합니다.
  - **Mondaine White**: 같은 시계의 흰 문자판.
  - **Digital**: 시를 좌상단, 분을 우하단에 크게 놓는 디지털 시계. 숫자는 사진에서
    떠낸 글리프를 그대로 씁니다.
  - **Weather Digital**: 현재 날씨 아이콘과 기온, 한글 날씨 이름, 시각.
  - **Date Digital**: 시각과 한글 요일, 년월일.
- 두 몬데인 화면에 바늘 그림자를 넣었습니다. 문자판뿐 아니라 아래 놓인 바늘에도
  드리웁니다. 회색을 칠하는 것이 아니라 이미 그려진 픽셀을 검정 쪽으로 섞는 방식이라
  흰 문자판은 어두워지고 그 위를 지나는 검은 눈금은 밝아지지 않습니다.
- 화면별 색 채널에 `accent`를 더했습니다. 넷으로 충분했던 것은 시계 얼굴뿐이고,
  날짜 화면은 배경 외에 잉크가 넷입니다.
- 번들 UI 폰트에 없는 한글 다섯 자를 따로 구워 넣었습니다. 폰트를 교체하지 않고
  조회가 빗나갈 때만 참조합니다.

### Changed

- 상단 메뉴의 `Analog` 탭 이름을 `Clock`으로 바꿨습니다. 이제 시계 화면 전부의 색을
  여기서 지정합니다.
- 색 카드가 고른 화면에 맞춰 채널 라벨을 바꾸고, 그 화면이 쓰지 않는 채널은 숨깁니다.
- `Display Settings` 카드를 `System` 메뉴 안으로 옮겼습니다.
- **웹 비밀번호를 관리 화면에만 걸리도록 좁혔습니다.** OTA, `/file`, `/api/*`,
  raw 8080 복구 경로는 종전대로 인증 없이 동작합니다. 기존 도구와 복구 절차를
  그대로 쓰기 위한 선택입니다.
- 날씨·날짜 화면의 시각을 7세그먼트에서 디지털 화면과 같은 폰트로 바꿔, 세 디지털
  화면이 한 계열로 보이도록 했습니다. 구운 셀을 박스 필터로 줄여 쓰므로 폰트가
  하나 더 들어가지는 않습니다.
- 날씨 이름과 날짜를 한글로 표기합니다. 날짜는 `2026년 08월 20일` 형식입니다.

### Fixed

- 디지털 화면 숫자의 아래쪽이 잘려 보이던 문제를 고쳤습니다. 글자를 세로 중앙에
  맞추면 폰트의 중앙선 기준이라 잉크가 박스를 넘칩니다. 잉크 기준으로 잘라내도록
  바꿨습니다.
- 날씨 화면이 프레임마다 아이콘 파일을 띠 수만큼 읽어 746ms가 걸리던 문제를
  고쳤습니다. 아이콘은 띠를 다 올린 뒤 화면에 한 번만 그립니다. 117ms로 줄었습니다.
- 몬데인 화면에서 바늘이 그려질 세로 구간을 계산할 때 아날로그 화면의 중심을 쓰던
  것을 고쳤습니다.
- 그림자 래스터라이저가 도형의 바운딩 박스 전체를 훑던 것을 스캔라인 방식으로
  바꿨습니다. 프레임당 117ms에서 40ms로 줄었습니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.3.bin`
  - SHA256: `F7CDBD077134DACADEAF89BF2636F9430AD516F1EEECACA8D1906E97CB9ACD37`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.3.bin`
  - SHA256: `EB3FD50785154874B51696A7A39B90AFDC732FB86B7726B0C8DBF60605251343`
- 플래시 사용량 59.0%, RAM 43.8%.

### Notes

- LittleFS 이미지는 빌드마다 바이트가 달라집니다. 위 해시는 이 릴리스에 실제로
  올라간 이미지의 것입니다.
- 전체 LittleFS 업로드는 설정을 지웁니다. 웹 파일만 갱신할 때는 `/file?path=/web/...`을
  씁니다.

## [v1.0.2] - 2026-08-25

### Added

- 아날로그 시계 화면을 추가했습니다. 검은 문자판에 시간바 8개와 12·3·6·9 숫자, 끝으로 갈수록 좁아지는 바늘 세 개로 구성했습니다.
- 숫자는 시계/날씨 화면의 `ClockDigitFont` Main 글리프를 그대로 씁니다. 원본이 55픽셀 고정이고 확대·축소 기능이 없어 28픽셀로 박스 필터 축소해 넣었습니다.
- 화면 선택과 전환을 추가했습니다. 웹 UI에서 쓸 화면을 고르고 전환 간격을 지정합니다.
- 아날로그 시계의 문자판, 테두리, 숫자·시간바, 바늘 색을 웹 UI에서 지정할 수 있게 했습니다. 실제 표시될 RGB565 값을 함께 보여줍니다.
- 웹 UI에 비밀번호 로그인을 추가했습니다. 세션 토큰은 부팅할 때마다 새로 만듭니다.
- `/status`에 힙과 아날로그 프레임 시간을 노출해 성능을 장치에서 직접 확인할 수 있게 했습니다.

### Changed

- 웹 UI 제목을 `선일의 다목적 모니터링 디스플레이`로 바꿨습니다.
- CPU를 160MHz로 올렸습니다. 아날로그 화면 합성이 CPU에 묶여 있었습니다.
- `scripts/build_release.ps1`이 전체 소스 zip 생성과 GitHub Release 게시까지 처리합니다. 게시는 `-Publish`를 붙일 때만 합니다.

### Fixed

- 아날로그 화면을 오프스크린 스프라이트에 띠 단위로 합성한 뒤 밀어 넣도록 바꿔 깜빡임을 없앴습니다. 화면에는 완성된 픽셀만 나타납니다.
- 매초 바뀌는 띠만 다시 그립니다. 한 프레임이 250ms에서 60~130ms로 줄었습니다.
- `drawWedgeLine`이 선의 바운딩 박스 전체를 스캔하던 낭비를 없앴습니다. 획을 띠 안쪽 구간으로 먼저 자른 뒤 그립니다.
- 부분 갱신에서 분침이 두 위치에 동시에 그려지던 문제를 고쳤습니다. 느린 바늘은 화면이 확정한 각도로만 그립니다.
- 부팅 시 활성 화면을 설정에 맞춰 정하도록 고쳤습니다. 시계/날씨를 꺼둔 채 재부팅해도 그 화면이 나오던 문제입니다.
- OTA와 `/file`이 업로드 시작 시점에 인증을 확인하도록 했습니다. 완료 시점 확인은 이미 기록이 끝난 뒤라 의미가 없었습니다.
- 전체 화면을 덮는 시스템 화면을 그린 뒤 chrome 플래그를 지우도록 했습니다. OTA 실패 후 화면이 깨진 채 남던 문제입니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.2.bin`
  - SHA256: `90C0A65E6E4B199D7F0E3A06005C9966C8810C7ADB5D3BF6E2DAD443A9E229DF`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.2.bin`
  - SHA256: `76AD052D63659578BAD59BCD0BB21B7438B0C8519192C1D847E2DD108C9AD093`

### Notes

- LittleFS 이미지는 빌드마다 바이트가 달라집니다. 펌웨어 bin은 재현되지만 `mklittlefs` 산출물은 그렇지 않으므로, 위 SHA256은 실제로 릴리스에 올라간 빌드의 값이어야 합니다. 릴리스에 쓸 빌드에서 뽑은 해시를 그대로 넣으십시오.

### Known Limitations

- 패널이 16비트라 어두운 영역의 표현 단계가 매우 적습니다. 검정 바로 위 값은 네 개뿐이라 문자판과 테두리를 모두 어둡게 두면 서로 구분되지 않을 수 있습니다.
- 아날로그 화면은 초 단위로 갱신합니다. 부드러운 초침은 지원하지 않습니다.

## [v1.0.1] - 2026-08-25

### Added

- 자동 야간 모드를 추가했습니다. 지정한 시간대에 백라이트를 야간 밝기로 낮추고, 시작 시각이 종료 시각보다 늦으면 자정을 넘어가는 구간으로 처리합니다.
- 웹 UI System 메뉴에 Auto Night Mode 사용 여부, 야간 밝기, 시작/종료 시각 입력을 배치했습니다.
- `/api/config`에 `night_mode_enabled`, `night_brightness`, `night_start_minutes`, `night_stop_minutes` 항목을 추가했습니다.

### Changed

- 시계 화면의 날짜 타이포그래피 정렬을 조정했습니다.
- 시계/날씨 화면의 세로 간격을 조정했습니다.
- 시계/날씨 화면 텍스트 대비를 높여 가독성을 개선했습니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.1.bin`
  - SHA256: `DF3713B2DE7C3A7A580E1A852003F1C89E3858192F9CF8F1E53BF19B895FB8A2`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.1.bin`
  - SHA256: `7DD727A15DE90D00E0953A44457319699A54878B30F9B4087C9998F7515401A8`

### Known Limitations

- 야간 모드는 장치 로컬 시간 기준이므로 NTP 동기화 전에는 적용되지 않습니다.
- LCD 드라이버 연결부는 계속 `TFT_eSPI` 기반 어댑터입니다.

## [v1.0.0] - 2026-08-25

### Added

- SD_PRO / SmallTV 호환 ESP8266용 `SDP Clock Weather` 안정 펌웨어를 추가했습니다.
- 원본 `wonjj6768/smalltv-ultra-korean-custom-firmware`의 시계/날씨 화면 구성 요소를 프로젝트에 포함했습니다.
  - `ClockDashboardScene`
  - `ClockDigitFont`
  - `UiTextFont`
  - LittleFS BMP 날씨 아이콘
- 기상청 APIHub 기반 날씨 조회와 위치 격자 설정을 추가했습니다.
- 웹 UI를 Status, Weather, System, Recovery 탭으로 구성했습니다.
- System 메뉴에 WiFi SSID/password, 밝기, 재시작, LittleFS 목록 기능을 배치했습니다.
- Recovery 메뉴에 펌웨어 OTA, LittleFS OTA, raw 8080 복구 경로 안내를 배치했습니다.
- `/file?path=/...` 파일 단위 업로드 API를 추가해 전체 LittleFS를 덮지 않고 웹 파일/아이콘을 교체할 수 있게 했습니다.

### Fixed

- 복구 AP는 비밀번호 없이 열리도록 정리했습니다.
- ST7789 백라이트가 active-low인 보드에서 UI 밝기 값과 실제 밝기가 반대로 동작하던 문제를 보정했습니다.
- 공개 배포 빌드에서 하드코딩 WiFi SSID/password를 제거했습니다.

### Build

- 펌웨어 바이너리: `release/SDP_ClockWeather_v1.0.0.bin`
  - SHA256: `E95E72E217064864D487DE62818A69CA248B8DEAFA3CB3409F890F2210DCF111`
- LittleFS 이미지: `release/littlefs-clock-weather-v1.0.0.bin`
  - SHA256: `48812BA15D8E62001853040D1608C5C1BF4C1A4CE36139778721C1F8B627ECFF`

### Known Limitations

- 화면 구성 요소와 폰트/아이콘은 원본 자산을 사용하지만, LCD 드라이버 연결부는 현재 `TFT_eSPI` 기반 어댑터입니다.
- OTA가 불안정한 장치는 UART/TTL 복구 경로를 확보한 뒤 펌웨어와 LittleFS를 갱신해야 합니다.
